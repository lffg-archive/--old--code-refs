# `docker container start (...)`

Inicializa um ou mais _containers_ que não estão ativos no momento.

## Opções:

#### `--attach` (`-a`)

Anexa o _stdin_ e _stdout_ do _container_ ao terminal.

### `--interactive` (`-i`)

Anexa o _stdin_ do container ao terminal. "Modo interativo".
