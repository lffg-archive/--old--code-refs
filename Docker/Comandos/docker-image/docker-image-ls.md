`docker image ls`

Lista todas as imagens do computador.

```shell
docker image ls
```

## Opções:

#### `--all` (`-a`)

Mostra todas as imagens.

#### `--quiet` (`-q`)

Mostra somente as imagens instaladas.
